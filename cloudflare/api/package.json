{
  "name": "@follow/api-proxy",
  "type": "module",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run",
    "typecheck": "node --check ../cors.js && node --check ../snippet.js"
  },
  "devDependencies": {
    "vitest": "4.1.10",
    "wrangler": "4.107.1"
  }
}
